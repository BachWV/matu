#include<stdio.h>
void main()
{
	char str1[100], str2[100], c;
	int i = 0, s = 0;
	gets(str1);
	gets(str2); 
	while ((str1[i] == str2[i] && str1[i] != '\0')) i++;
	#include <stdio.h> #include <string.h>  int main (void)     {char j[100],k[100];      int i=0;      int a;      gets(j);      gets(k);      while(j[i]==k[i]&&j[i]!='\0')        {i++; 	   } 	 a=j[i]-k[i]; 	 printf("%d",a);      return (0); 	 } 
	printf("%d\n", s);
}
