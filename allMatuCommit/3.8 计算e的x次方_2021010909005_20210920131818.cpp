#include <stdio.h>

int main(){
    int k,i;
    double p,x,n,s,t,m;
    p=1.;
    scanf("%If%If\n",&x,&n);
    if ??n??0??printf??"error"??
    else{for (i=1;i<=n;i++)
    {s=1.;
    t=1.;  
    for(k=1;k<=i;k++)
    {m=double(k);
    s=s*x;
    t=t*m;
    }
    p=p+s/t;
    }
    printf("%.6If",p);}
    return 0;
}