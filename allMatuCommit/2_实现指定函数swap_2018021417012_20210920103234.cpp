#include <stdio.h>

void swap(int* array, int n)
{
	if (n > 0 && array != NULL)
	{
		int temp = *array;//??????§Ş???????array??????????
		*array = *(array + n - 1);//??????????????????????????
		*(array + n - 1) = temp;//???temp?????????????????
	}
	else
		printf("error");
}